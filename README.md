# chatbot-test
To run, use the following commands in the terminal:
`python -m venv .env` creates a new virtual environment in the current directory. `cd bot` navigates to the bot directory. `source .env/bin/activate` activates the virtual environment. `python main.py` runs the chatbot. Currently using `microsoft/DialoGPT-large` model.